<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Final_Insa</title>
    <script src="https://kit.fontawesome.com/2b40017a14.js" crossorigin="anonymous"></script>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.2/dist/css/bootstrap.min.css" integrity="sha384-xOolHFLEh07PJGoPkLv1IbcEPTNtaed2xpHsD9ESMhqIYd0nLMwNLD69Npy4HI+N" crossorigin="anonymous">
    <link rel="stylesheet" href="/public/home.css">
</head>

<body>
    <nav class="navbar navbar-expand-lg navbar-dark">
        <div class="container-fluid">
            <a class="navbar-brand" href="#">Portfolio</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse"
                data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false"
                aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="nav-link" href="/home">Home</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/logout">Logout</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/dashboard">Admin panel</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <div class="container">
        <h3>Welcome, <%= user.username %>!</h3>
        <p>Glad to see you back. Explore what's new today!</p>

        <main>
            <div class="row">
                <% if (posts) { %>
                    <% posts.forEach(post => { %>
                        <div class="col-md-6">
                            <%- include('./portfolio/list.ejs', { post: post, user: user }) %>
                        </div>
                    <% }) %>
                <% } %>
            </div>
        </main>

        <section class="news-section">
            <h2>Latest News</h2>
            <div class="news-container">
                <!-- News items will be dynamically added here -->
            </div>
        </section>
    </div>

    <footer class="fixed-bottom">
        <div>
            <h6>© 2024 Insaniyat Tungushpayeva<a href="https://github.com/Ssberry19" target="_blank">
                    <i class="fab fa-github"></i> </a></h6>
        </div>
    </footer>

    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.5.3/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
    <script>
       $(document).ready(function () {
    if (!process.env.NEWS_API_URL) {
        console.error('News API URL is not defined.');
        return;
    }

    $.ajax({
        url: '<%= process.env.NEWS_API_URL %>',
        method: 'GET',
        success: function (data) {
            console.log('News API response:', data);
            var articles = data.articles;
            var newsContainer = $('.news-container');

            articles.forEach(function (article) {
                var newsItem = $('<div class="news-item"></div>');
                var title = $('<h3>' + article.title + '</h3>');
                var description = $('<p>' + article.description + '</p>');
                var source = $('<p>Source: ' + article.source.name + '</p>');

                newsItem.append(title, description, source);
                newsContainer.append(newsItem);
            });
        },
        error: function (xhr, status, error) {
            console.error('Error fetching news:', error);
            $('.news-section').html('<p>Error fetching news. Please try again later.</p>');
        }
    });
});

    </script>
</body>

</html>
